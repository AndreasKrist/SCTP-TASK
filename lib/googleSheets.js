// lib/googleSheets.js
// Stubbed out — Google Sheets integration replaced by Firebase
export async function saveToGoogleSheet(userData) {
  return { success: true, message: 'Google Sheets save skipped (Firebase pending)' };
}